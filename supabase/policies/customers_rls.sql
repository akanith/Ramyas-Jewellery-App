-- ============================================================
-- Row Level Security Policies — Customers Table
-- ============================================================
-- These policies ensure customers can only access their
-- own data, and admins have full access.
-- ============================================================
-- NOTE: Actual policy SQL will be implemented during the
-- database schema phase.
--
-- Planned policies:
--   customers_select_own      — Customer reads own row
--   customers_update_own      — Customer updates own profile
--   customers_admin_all       — Admin has full access
-- ============================================================

-- Placeholder — to be implemented
SELECT 'RLS policies placeholder — customers' AS status;
