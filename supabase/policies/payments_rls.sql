-- ============================================================
-- Row Level Security Policies — Payments Table
-- ============================================================
-- Planned policies:
--   payments_select_own       — Customer reads own payments
--   payments_admin_all        — Admin has full access
--   payments_insert_admin     — Only admin can record payments
-- ============================================================

SELECT 'RLS policies placeholder — payments' AS status;
