-- ============================================================
-- Row Level Security Policies — Schemes Table
-- ============================================================
-- Planned policies:
--   schemes_select_own        — Customer reads own scheme enrollments
--   schemes_admin_all         — Admin has full access
-- ============================================================

SELECT 'RLS policies placeholder — schemes' AS status;
