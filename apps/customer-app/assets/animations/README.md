# Place Lottie animation JSON files here.
# Used with the lottie Flutter package.
#
# Planned animations:
#   loading.json            — Loading / spinner animation
#   success.json            — Payment success celebration
#   scheme_complete.json    — Scheme completion animation
#   empty_state.json        — No data empty state
