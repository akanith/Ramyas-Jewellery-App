// ============================================================
// Admin Dashboard — App Configuration Constants
// ============================================================
// config/ stores non-secret, environment-agnostic config.
// ============================================================

// Planned config files:
// config/
//   app-config.ts         — App name, version, pagination defaults
//   navigation.ts         — Sidebar nav links definition
//   permissions.ts        — Role-based access control definitions
//   scheme-defaults.ts    — Default scheme plan parameters
//   table-columns.ts      — Shared DataTable column definitions

export {};
